import type { Settings } from '../types'

export const DEFAULT_SETTINGS: Settings = {
  breakInterval: 20,
  breakDuration: 20,
  blinkThreshold: 12,
  stareDelay: 20,
  cameraFps: 24,
  chartInterval: 60,
  soundEnabled: true,
  nativeNotifications: true,
}
